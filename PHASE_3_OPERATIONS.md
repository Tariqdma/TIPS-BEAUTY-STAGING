# تشغيل المرحلة الثالثة

تضيف المرحلة الثالثة ثلاثة أجزاء مترابطة: لوحة إدارة متجاوبة للهاتف، إدارة الفروع والمخازن، وبوابة المندوب. قاعدة البيانات مطبقة على Supabase في migration باسم `phase3_multi_warehouse_and_driver_portal`.

## الفروع والمخازن

من بوابة الإدارة، افتح **الفروع والمخازن**. أضف كل فرع باسم، رمز مختصر فريد، الولاية والمدينة. بعد ذلك، استخدم نموذج **تعديل مخزون فرع** لإضافة رصيد كل منتج للفرع. الرصيد الظاهر في كتالوج المنتج يصبح مجموع الرصيد في كل الفروع عند بدء استخدام مخزون الفروع.

يمكن تحويل الكميات بين فرعين من نموذج **تحويل مخزون بين الفروع**. تنفذ العملية بصورة ذرية: لا تُضاف كمية إلى الوجهة ما لم تتوفر في الفرع المصدر، وتُسجل حركة دخول وخروج وسجل التحويل.

عند وجود مخزون فرعي، يختار `create_order` مخزناً نشطاً لديه كل عناصر الطلب، مع تفضيل المدينة والولاية المطابقتين للعميل. إذا لم تضف أي رصيد فرعي بعد، يستمر المتجر في استخدام حقل المخزون الحالي للمنتجات حتى لا يتوقف التشغيل الانتقالي.

## إعداد مندوب جديد

إنشاء سجل المندوب وحده لا يكفي لتسجيل الدخول. يلزم ربط سجل المندوب بحساب مصادقة وصلاحية `driver`.

1. من Supabase، افتح **Authentication → Users** وأنشئ حساب المندوب أو اجعله يسجل حسابه.
2. انسخ User ID للحساب.
3. من **SQL Editor** نفذ الاستعلام التالي بعد استبدال القيم:

```sql
UPDATE public.profiles
SET role = 'driver'
WHERE id = 'USER_UUID';

UPDATE public.drivers
SET user_id = 'USER_UUID'
WHERE id = 'DRIVER_UUID';
```

4. افتح بوابة المندوب وسجل الدخول بالبريد وكلمة المرور نفسهما.

لا تشارك كلمة المرور أو مفاتيح Supabase في أي مكان.

## صلاحيات المندوب

المندوب يرى فقط الطلبات المعينة له عبر حقل `orders.driver_id`. يستطيع تغيير حالته المتاحة إلى متاح أو غير متصل، ويستطيع فقط تحديث طلباته إلى **خرج للتوصيل** أو **تم التوصيل**. كل تغيير ينشئ سجلاً في `order_status_history`. لا يمكنه تعديل السعر أو المخزون أو المنتجات أو رؤية طلبات مناديب آخرين.

## روابط النسخة التجريبية

| الواجهة | الرابط |
|---|---|
| متجر العملاء | `https://tariqdma.github.io/TIPS-BEAUTY-STAGING/` |
| بوابة الإدارة | `https://tariqdma.github.io/TIPS-BEAUTY-STAGING/admin/` |
| بوابة المندوب | `https://tariqdma.github.io/TIPS-BEAUTY-STAGING/driver/` |

استخدم الرابط الأساسي للبوابتين، ثم التنقل داخل التطبيق. بوابة الإدارة والمندوب تستخدمان Hash Router لتجنب خطأ 404 عند إعادة التحميل على GitHub Pages.
